---
title: Getting started
description: Prerequisites and first run of the Documind application.
---

## Prerequisites

- **Node.js** 20+ (22+ recommended)
- **npm** (or compatible package manager)
- **Docker Desktop** (or Docker Engine + Compose) for Postgres, Redis, RustFS, Qdrant
- Git

## Clone and open the app

```bash
cd C:\Users\Aniket\Documents\GitHub
cd documind
```

## First-time setup

1. Copy environment file and set secrets:

```bash
cp example.env .env
```

Edit `.env`: set `BETTER_AUTH_SECRET` and **`GEMINI_API_KEY`** (Google AI Studio). Never commit real keys.

2. Start infrastructure:

```bash
docker compose up -d
```

3. Install dependencies and push the schema:

```bash
npm install
npm run db:push
```

4. Start the app (ingest runs in Docker already):

```bash
npm run dev
```

5. Open [http://localhost:3000](http://localhost:3000), create an account, and try **New chat**.

## What “done” looks like

| Check | Expected |
|-------|----------|
| App | Marketing home + login/signup |
| After login | Redirect to `/new` |
| Docker | Postgres, Redis, RustFS, Qdrant, **pdf-extract `:8090`**, **ingest-worker** |
| Documents | Upload a `.txt` or **text PDF** → status **`indexed`** |
| Chat | Pin that file to the chat → ask a question → **streamed** answer |
| Redis demo | CRUD on `/redis` |

## Processes

| What | How |
|------|-----|
| Infra + worker + PDF extract | `docker compose up -d` |
| Next.js app | `npm run dev` |

Details: [Document ingest](/docs/guide/document-ingest) · [PDF extract](/docs/guide/pdf-extract) · [RAG chat](/docs/guide/rag-chat).

## Next

- [Installation](/docs/guide/installation) — detailed steps  
- [Local development](/docs/guide/local-development) — daily workflow  
- [Environment](/docs/guide/environment) — all env vars  
